REQUIREMENTS:
	NLTK w/ data download. If receiving errors about NLTK corpus, run:
		>> import nltk
		>> nltk.download()
	from python interactive sessions

EXECUTION:
	Run:
		python grader.py <directory path>
	This will run the grader on all files inside the provided directory